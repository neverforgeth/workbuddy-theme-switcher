# 1.6.0 候选版：本机验收补充记录

日期：2026-09-15。本记录补充首轮 `1.6.0-validation.md`，不把浏览器样板测试等同于实机验收。

## 授权与环境

- 用户先允许可回退实机验收，随后单独允许重启 WorkBuddy 以开启 CDP；未授权覆盖安装，本轮不安装。
- 正常关闭请求后 WorkBuddy 仍驻留后台；核对 `D:\workbuddy\WorkBuddy.exe` 的主进程及子进程后结束，并重新启动。
- 启动参数为 `--remote-debugging-address=127.0.0.1 --remote-debugging-port=19336`，环境变量 `WORKBUDDY_REMOTE_DEBUGGING_PORT=19336`；监听地址确认仅为 `127.0.0.1`。
- 实机版本 5.2.6.0，CDP renderer 为该安装目录下 `app.asar/renderer/index.html`。不承诺其他版本。
- 没有运行中的旧切换器。新测试进程使用本次源码重新编译的 Debug 1.6.0，测试入口不编译进 release 安装包。
- 独立数据目录：`C:\Users\EDY\Documents\Codex\2026-08-31\w-s\.qa\live-160-20260915`。不读写真实凭据，不修改正式主题库、自动保持设置或开机启动项。
- 正式设置保存的意图为 `ice-blue` / 自动保持 true；重启后实际探测为原版、样式节点 0。测试以实际探测的原版为回退基线，不把保存意图冒充当前主题。
- 正式 `state.json` 原始 SHA-256：`C3EA7CBE66632CECDEE3AF6DAA14AA2DFA9505B95F819341B0873E26BFB4B7E9`。
- 最初页面包含聊天，截图预检被安全检查拦截；没有用其他方式绕过。用户随后手动切到空白首页并明确允许本机截图；不自动切换会话、不提取聊天文本、不上传截图。

## 已确认结果

| 检查 | 结果与边界 |
| --- | --- |
| CDP 连接 | 通过；监听仅限本机 |
| 实机基线 | 原版，主题节点 0，恢复目标明确 |
| 缓存草稿编辑 | 40 次，后端更新/编译/原子暂存 P95 9.2073ms |
| 连续实机同步 | 40 次，编辑+CSS 同步+节点验证 P95 49.1822ms，最新序号均一致 |
| 保存再保留 | 通过；保存不直接结束试用，确认后使用同一修订 |
| 关闭编辑窗口回退 | 通过，恢复固定修订 |
| 断连待恢复 | 通过；只将隔离测试的端口暂设为 19337，不关闭 WorkBuddy、不修改系统网络 |
| 重连恢复 | 通过，恢复固定修订 |
| 10 分钟超时 | 实际计时中，尚不宣称通过 |

上述性能不含前端防抖、WebView IPC/绘制及截图，不能宣称完整端到端 P95 已达标。

## 待完成

- 超时回退、最终原版节点 0 和正式状态哈希复核。
- 用户确认的空白首页图片主题截图与逐张视觉检查。
- 崩溃恢复记录验收。
- 本轮不自动打开聊天、菜单、弹窗；未验证场景保持待验收。
- 实际 WebView CSP、断开外网时完整 UI 流程、安装升级、完整帧呈现性能及输入/滚动人工验收仍独立记录；安装包暂保持候选版。

# 1.5.0 验收记录

更新日期：2026-09-08。实际目标：`D:\workbuddy\WorkBuddy.exe`，文件版本 5.2.6.0，环回 CDP 19336。

## 本轮范围与边界

按用户最新要求，本轮只完善已经实现的功能，不继续扩展产品范围。重点是连续编辑、截图超时和过期标记、关闭/取消、保存事务与草稿保留。

9 月 8 日恢复显示 WorkBuddy 后，只读截图诊断获得约 198ms 的 PNG 返回；最小化时出现暂停绘制和超时。这证明了最小化相关原因，不等于新版试穿效果已经通过验收。之后用户按 Esc 中止桌面操作；未运行新的实机试穿矩阵，未重启 WorkBuddy，未调用收费 AI，也未覆盖安装。原安装版切换器已在中止前关闭。

以下本机试用、超时、崩溃恢复数据来自 9 月 7 日旧候选构建，不能替代 9 月 8 日修改后的实机复测。当前交付仍应标记为候选版。

## 状态

已实现真实截图主预览、九区设计、一次性 AI 发送审核、连续试穿和不可变修订。**完整验收尚未结束，不应将下面的浏览器样板测试视为真实截图验收。** 未覆盖安装，也未调用收费 AI。

## 自动回归

- Rust：69 项通过，0 失败、0 忽略。包括图片限制、旧主题修订兼容、低对比度修正、手调强调色、10 分钟续期、HTTPS/地址限制、网络超时、非法 AI JSON、禁重定向、数据不出错误信息及不透明遮挡；新增保存提交失败/崩溃恢复、重命名保留草稿、连续分片和握手超时、关闭后拒绝迟到任务、审核凭证单次消费，以及 AI 请求只携带已审核图片与语义信息的本机假服务测试。
- React/Vitest：19 项通过。包括独立 AI 配置草稿、连续参数编辑、试穿中编辑不保存正式主题/不请求 AI、丢弃过期截图、审核确认门槛、分区提交期间禁止保留、失败编辑保留与重试，以及准备/遮挡/发送期间取消后的迟到结果保护。
- Edge/Playwright：20 项通过。包括五类图片三种辅助场景、900/1200 窗口宽度、截图容器的适应宽度/100% 局部滚动、单节点与背景 URL 不变、示例输入和滚动保留、暂停动画帧不阻塞、几何探针不返回文字及敏感图元遮挡。新增整页 App 在 900×660 与 1200×820 的合成测试：没有横向溢出、四项参数可编辑、主预览节点不卸载，AI 请求、正式主题写入和外部请求均为 0。
- TypeScript 类型检查、ESLint、Vite 生产构建及 NSIS 安装包构建通过。

本轮桌面操作暂停后，所有网络回归只使用测试进程创建的环回假服务和本机测试页面，不连接真实 WorkBuddy，也不向外部 AI 发送任何内容。整页测试截图明确标注“合成测试帧·非 WorkBuddy 实机截图”，只用于检查布局。

### 本轮完善的具体问题

- 连续全局/分区编辑使用同一队列；失败不抹掉用户输入，也不允许保存旧效果。截图不再占用样式同步锁，失效画面不会显示为最新结果。
- 关闭窗口或取消审核会使旧任务失效；旧截图、旧审核响应及排队中的试穿不能重新激活已关闭的流程。
- 保存以 `head.json` 为唯一提交点，失败回滚草稿元数据，新版未发布修订不会在重试时被误认为保存成功。重命名不再抢占最近草稿入口。
- CDP 握手和持续分片读取增加 I/O 截止时间；最小化未绘制时返回可处理错误，不宣称获得了新截图。此为 I/O 边界，不是整条截图流程的硬实时承诺。

## 历史本机结果（2026-09-07，独立数据目录，不写生产主题库）

- 原主题：ice-blue 1.0.4，自动保持开启，初始主题节点 1。
- 连续 40 次后端更新 + 实机 CSS 更新 + 校验：多轮 P95 24.2–40.2ms；单独草稿编译及原子暂存 P95 10.1–12.8ms。最终扩展叠图/父面板抽样的草稿编译 P95 为 12.7ms；完整实机更新 40 次数据来自扩展抽样之前。此值**不含前端防抖、IPC、绘制与截图**，不是端到端 P95 承诺。
- 保存并保留通过；保存时仍是临时主题，确认后为同一已保存修订。
- 关闭编辑窗口触发恢复通过。
- 将隔离测试配置端口切为不可用端口，取消返回待恢复；恢复正确端口后自动恢复通过。没有关闭或重启 WorkBuddy。
- 完整 10 分钟超时通过：18:55:10 开始，19:05:12 验证恢复，19:05:13 恢复原版时节点为 0，19:05:15 恢复原冰蓝主题并确认节点为 1。
- 崩溃恢复通过：19:07:05 在一次快更新后退出隔离测试进程；19:07:33 新进程读取持久化记录并验证恢复 ice-blue。旧主题混合目录、复制及应用亦通过。
- 测试后已重新打开原安装版 1.4.0。WorkBuddy 七个进程仍为 9 月 4 日的同一批 PID/启动时间，未被重启；测试结束时生产 state.json 与测试前备份 SHA-256 一致。
- 实际截图：隐藏渲染状态下出现截图超时，自动恢复原冰蓝主题已验证。已修复无界 RAF 等待，并将截图失败显示为可处理的“显示 WorkBuddy 后刷新”；真实图片的最终成功截图和脱敏人工复核仍待窗口恢复绘制后验证。

数据备份：`C:\Users\EDY\Documents\Codex\2026-08-31\w-s\upgrade-backups\pre-150-qa-20260907`。`.qa/runtime-live-150*` 和 `.qa/runtime-lifecycle-150` 是本次隔离验收数据，不进入源码包或安装包。

## 明确限制 / 尚未完成

- 主预览确实调用 WorkBuddy 的截图接口，不生成效果图；但窗口最小化或完全遮挡时 Electron 可能暂停绘制，需显示/并排放置后刷新。不会替用户导航聊天或自动强制重启。
- 未使用真实 Key 调用外部视觉模型，因此不宣称 AI 的审美质量已经实测通过；成功/失败契约以本机模拟接口测试验证。
- 脱敏只允许 5.2.6 / 5.2.6.0 且结构被识别的当前可见页面；其他版本/未知结构拒绝外发。自动遮挡不能代替用户检查审核副本。
- 颜色对比度是叠图和父面板的抽样检查。主背景保留透景，不用大面积不透明底色强行刷出“全部通过”；主背景警告及动态继承/未覆盖状态如实显示未验证。
- 真实截图五类图片、多场景人工视觉复核、原始截图导出文件对话框仍待验收。旧版结果不可当作本轮新版结果。
- 旧版没有发布标记的完整历史修订保持兼容，无法仅凭旧文件回溯区分过去的完整孤儿修订；本版失败修订有明确的未发布标记。
- 候选安装包已生成，未代码签名；必须先补齐关键实机截图验收，再决定是否安装替换稳定版。本轮源码和候选包放在 `delivery/1.5.0-polished/`，不自动安装；旧 `1.5.0-candidate/` 保留，但不代表本轮最新代码。

## 可重复测试入口

`STUDIO_QA_ROOT` 仅在 debug 构建改变数据根目录；`STUDIO_RUN_QA=live` 运行截图/遮挡/试穿闭环，`lifecycle` 只跑无需截图的保留、断连和真实 10 分钟回退，`crash` / `recover` 验证进程退出后的恢复记录。调用前必须经用户批准并关闭其他切换器。所有入口禁止重启 WorkBuddy，并尝试恢复起始主题。

`scripts/diagnose-live-capture.mjs` 是只读诊断工具，只返回可见性、尺寸、掩码数量及请求耗时，不打印原始截图、聊天内容或密钥。
